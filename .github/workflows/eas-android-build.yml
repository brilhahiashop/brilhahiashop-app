name: Optional EAS Cloud Build

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the GitHub-native APK workflow for production builds. EAS cloud build is optional."
